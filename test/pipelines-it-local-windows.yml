jobs:
- job: 'integration_test_local_windows'

  steps:
  - script:
      whoami && set path=$(path)  
      python -m pip install --upgrade pip setuptools --user
    displayName: 'Install python tools'
