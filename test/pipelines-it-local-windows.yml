jobs:
- job: 'Test'

  steps:
  - task: PowerShell@2
    continueOnError: true
    inputs:
      targetType: 'filePath'
      filePath: install.ps1
    displayName: 'Install nni toolkit via source code'