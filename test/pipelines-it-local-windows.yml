jobs:
- job: 'Test'

  steps:
  - task: UsePythonVersion@0
    inputs:
      versionSpec: '3.6' 
      addToPath: true 
      architecture: 'x64'
  - task: PowerShell@2
    inputs:
      targetType: 'inline' # Optional. Options: filePath, inline
      script: Write-Host "Hello World"
    displayName: 'install nni from source code'